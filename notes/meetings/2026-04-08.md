# Meeting notes, 2026-04-08

## Main outcome

We clarified that the project should not be framed as a direct Dijkstra-versus-Markov competition. Instead, the study has a clear hierarchy:

- **Dijkstra** is the main validated framework
- **Markov** is a secondary exploratory extension

The main paper direction is now much sharper than at the start of the discussion.

## Refined scientific framing

The project is now centered on four linked themes:

1. **Square versus hexagonal grids**
   - explicitly compare the previous square-grid framework against a new hex-grid framework
   - aim to improve realism and possibly predictive fit, especially at high latitudes

2. **Decadal mean validation**
   - first benchmark both grid types using decadal mean seasonal fields
   - compare simulations against the decadal mean observed tern flyway from the first paper

3. **Interannual variability and behavioural flexibility**
   - use yearly seasonal mean climate fields
   - quantify annual deviations from the decadal mean flyway
   - test how changing movement weights affects the ability to maintain a similar flyway across years

4. **Markov accessibility extension**
   - start from a Southern Ocean mask
   - no predefined destination
   - use Markov transitions to explore climatically accessible movement space
   - treat this as exploratory and biogeographical, not as the main validation core

## Important conceptual decisions made

- The main biogeographical point is that **climate sets the limits or boundaries within which birds operate**.
- The Markov framework is meant to show **climatically accessible regions** where birds could move, including regions not yet observed in tracking data.
- The first paper’s limitations that this sequel should address are:
  - too path-centric
  - not enough route uncertainty
  - inability to represent corridor width
  - too coarse spatial structure
  - geometric problems of square grids near the poles
- For the Markov model, one step currently means **one movement to a neighboring hexagon**.
- Seasonal-average climate is assumed fixed over the migration period, approximately 90 days.
- For Dijkstra, birds are assumed to have a destination.
- For Markov, birds do not have a predefined destination.
- Markov is not better or worse than Dijkstra, it answers a different question.

## Stronger paper theme identified during discussion

A stronger central paper theme emerged:

**flyway stability under interannual climatic variability and the behavioural flexibility required to maintain similar flyways through time**

This may become the main scientific contribution, with the hex-grid comparison as the methodological improvement and Markov as the exploratory extension.

## Experimental design choices discussed

### Environmental data structure
- use seasonal averages from individual years
- compare annual flyways to the decadal mean flyway
- use the decadal mean benchmark first for the square-versus-hex comparison

### Validation references
Two reference objects should be distinguished:
- the **observed decadal mean flyway** as the biological benchmark
- the **simulated decadal mean flyway** as the climate-model baseline

### Metrics
- RMSE to the decadal mean flyway remains the main candidate metric
- from the 2025 paper, RMSE was calculated by comparing the median longitudinal position of each population and each simulated flyway per 10° of latitude
- therefore, using median longitude deviation by latitude band as a secondary metric would be too redundant if defined in the same way
- a genuinely complementary secondary metric is still needed, likely something like endpoint / destination-region error or another simple positional summary not identical to the RMSE construction

### Weight sets
- the user suggested testing different weight combinations for wind, food, crosswind, and distance
- after checking the 2025 paper, a clearer decision emerged: begin the sequel with the same weighting logic used there for comparability
- this means:
  - four components: parallel wind, crosswind, distance, food
  - weights sum to 1
  - increments of 0.1
  - wind and distance weights can vary between 0 and 1
  - crosswind and food weights can vary between 0 and 0.5
- for the final comparison this points toward the same 195 combinations as the original framework
- for the early prototype phase, a smaller representative subset is preferable to avoid overbuilding too early
- a practical first prototype subset was agreed as good enough without further refinement to the exact best-performing 2025 combinations
- candidate prototype set:
  1. `1.0, 0.0, 0.0, 0.0` wind only
  2. `0.0, 0.0, 1.0, 0.0` distance only
  3. `0.5, 0.0, 0.5, 0.0` wind + distance
  4. `0.5, 0.0, 0.0, 0.5` wind + food
  5. `0.0, 0.0, 0.5, 0.5` distance + food
  6. `0.7, 0.0, 0.3, 0.0` wind-dominated
  7. `0.3, 0.0, 0.5, 0.2` distance + food leaning
  8. `0.7, 0.1, 0.2, 0.0` wind-dominated with some crosswind
  9. `0.4, 0.1, 0.3, 0.2` balanced four-component mix
  10. `0.5, 0.2, 0.3, 0.0` crosswind-inclusive wind-distance mix

### Starting condition for Markov
- initial idea: a Southern Ocean mask for non-breeding arctic terns
- this also allows testing sensitivity to starting location

### Scope control
- begin with **one tern population** first
- do not start with all populations at once

## Additional decision from later follow-up

- for the first prototype and benchmark phase, use the decadal mean environmental setup from the present flyways paper rather than rebuilding a new surface immediately
- the user explicitly wants this new project implemented in **Python only**
- therefore, old R notebooks are useful as methodological references, but the sequel should be rebuilt in Python rather than ported as an R workflow

## Reusable first input package identified from the guide scripts

The guide scripts were sufficient to infer the likely first reusable inputs for the Svalbard spring prototype.

Core decadal-mean inputs available in both `core_guide/Publication/` and `supporting_data/derived_outputs/` of the previous project:
- `springMeanSpeed.csv`
- `chlSpring.csv`
- `gdf_SS_10.csv`
- `gdf_NS_10.csv`
- `coefflist.csv`
- `SScoordinates.csv`
- `NScoordinates.csv`

Immediate interpretation:
- the first new prototype likely needs `springMeanSpeed.csv`, `chlSpring.csv`, `gdf_SS_10.csv`, and `coefflist.csv`
- `NS` files are useful immediately after, since Netherlands spring is the second planned case and uses the same seasonal surface
- the old coordinate outputs should be treated as benchmark artifacts, not as new-model inputs
- these benchmark files have now been copied into the new project under `data/raw/benchmark_from_2025/` so the sequel can remain self-contained

Environmental input structure checked explicitly:
- `springMeanSpeed.csv` is not just a speed raster export, it contains columns:
  - `lat`
  - `lon`
  - `speed`
  - `u10`
  - `v10`
  - `dir`
- therefore the spring wind CSV already includes both vector components and derived quantities, not only a single scalar speed field
- `chlSpring.csv` contains:
  - `lat`
  - `lon`
  - `chlor_a`
- both files appear to be regular gridded CSV tables with one row per grid cell and an extra index column that should be dropped in Python preprocessing

Important notation warning from the old workflow:
- in the old R notebook, the coefficient letters were assigned as:
  - `a = wind`
  - `b = distance`
  - `c = food`
  - `d = crosswind`
- in the later paper framing / Python post-processing, the conceptual ordering was presented differently:
  - `a = wind`
  - `b = crosswind`
  - `c = distance`
  - `d = food`

Decision for the sequel:
- follow the Python / paper ordering only
- translate old R-based ordering explicitly when needed, but do not reuse it as the new internal convention

## First implementation step agreed and started

The user asked that implementation prioritize:
- simplicity
- clarity
- reproducibility
- minimal reliance on fancy packages
- explicit documentation and readable reasoning at each step

Preferred Python stack for the sequel:
- `numpy`
- `pandas` for CSV / tabular work
- `geopandas` for geospatial handling
- `matplotlib` for figures generated by scripts and included in reports
- `xarray`
- and `H3` for the hexagonal grid

Reporting rule added:
- when a script is run, it should produce outputs the user can inspect quickly
- default expectation:
  - short text summary in the terminal
  - a small numeric summary table in `results/tables/`
  - 1 to 3 quick-look figures in `results/figures/`
- for substantial steps, also create a short Markdown report in `results/reports/`
- PDF export is considered a useful optional second step, but Markdown is the primary source format
- silent data generation without readable reporting is not enough for this project

Based on that, the first implementation step has been defined as:
- clean the copied benchmark environmental CSVs in Python
- create canonical benchmark tables before any grid remapping or modelling

Implementation scaffold created:
- `src/scripts/01_preprocess_benchmark_inputs.py`
- `src/notebooks/01_preprocess_benchmark_inputs.md`
- `src/scripts/02_inspect_benchmark_tables.py`
- `src/scripts/03_build_square_grid.py`

Current behavior of the first scripts:
- `preprocess_benchmark_inputs.py`
  - reads `springMeanSpeed.csv` and `chlSpring.csv`
  - drops the extra export index column if present
  - standardizes headers conservatively
  - writes cleaned outputs to `data/processed/benchmark_tables/`
- `inspect_benchmark_tables.py`
  - inspects the cleaned benchmark tables using pandas
- `build_square_grid.py`
  - defines the first square benchmark grid from the cleaned wind table

Environment setup completed:
- a project-local virtual environment was created at `.venv`
- installed packages include:
  - `numpy`
  - `pandas`
  - `matplotlib`
  - `xarray`
  - `h3`

Results from the first inspection run:
- both cleaned environmental tables have 64,800 rows
- coordinate coverage is global:
  - longitude from -180 to 179
  - latitude from -89.5 to 89.5
- both benchmark tables contain 17,479 missing-value cells in the environmental variables, which are likely ocean/land-mask or missing-data effects and must be handled explicitly later
- the benchmark support appears to be a 1° by 1° global grid

Results from the first square-grid builder run:
- the square benchmark grid was written successfully to `data/processed/grids/square_grid_from_benchmark.csv`
- inferred benchmark spacing is:
  - 1.0° in longitude
  - 1.0° in latitude

## H3 resolution selection step completed

Decision basis:
- the benchmark environmental export is on a 1° global lat-lon grid
- for long-term reuse beyond the Atlantic, the user preferred choosing the H3 level by matching the **global-mean** 1° cell area rather than a flyway-specific latitude

Implementation:
- created `src/scripts/04_choose_h3_resolution.py`
- estimated the global-mean area of a 1° x 1° cell using a spherical Earth formula
- compared that target against H3 average cell areas across resolutions 0 to 15
- wrote the full comparison table to `data/processed/grids/h3_resolution_comparison.csv`

Result:
- estimated global-mean 1° cell area: **7871.39 km²**
- closest H3 average-area match among standard resolutions: **H3 resolution 3**
  - average H3 cell area: **12393.43 km²**

Interpretation:
- the match is only rough, not tight
- H3 resolution 3 is substantially coarser than the global-mean 1° target in area terms
- H3 resolution 4 is already much finer than the target
- therefore the square-versus-hex comparison will inevitably be approximate rather than exact if limited to a single standard H3 level

Decision:
- proceed with **H3 resolution 3** for the benchmark comparison
- acknowledge explicitly in the methods that this is the closest standard H3 match to the benchmark grid in global-mean area terms, not an exact equivalent

## Open issues raised

- how to make the square-versus-hex comparison fair
- whether crosswind should be included in the first coarse model set or added later
- what the exact secondary metric should be alongside RMSE
- whether grid geometry and polar distortion should be quantified directly, not only predictive fit
- whether the Markov process should allow staying in place
- how broad the Southern Ocean starting mask should be
- whether the project is becoming too broad for one paper

## Process decision for future meetings

After each substantive discussion, the workflow should be:

1. write dated meeting notes
2. update relevant concept / technical / overview documents
3. overwrite the rolling `NEXT_MEETING_QUESTIONS.md` file with the current open agenda

This process was explicitly agreed and should be followed from now on.
