# Meeting notes, 2026-04-09

## Main outcome

The discussion shifted from project architecture into concrete prototype implementation and environment/memory workflow refinement.

## Main scientific / technical decisions made today

### 1. Preferred stack clarified further
For this project:
- `pandas` for CSV / tabular work
- `geopandas` for geospatial handling
- `xarray` for multidimensional and NetCDF work
- `matplotlib` for script-generated report figures
- `H3` for the hexagonal grid

### 2. Benchmark support and H3 comparison
- the benchmark environmental package is on a 1° global lat-lon grid
- H3 resolution should be chosen by global-mean area matching rather than a flyway-specific latitude
- a global-mean 1° cell area comparison was run
- the agreed first benchmark choice is **H3 resolution 3**
- this must be described explicitly as an approximate match, not an exact equivalent to the 1° grid

### 3. Reporting refinements
The reporting workflow was strengthened:
- figures should be embedded directly in reports when they help understanding
- ordered workflows should use numbered script/report names
- short Markdown milestone reports remain the preferred human-readable reporting format

### 4. Geometry workflow completed further
New implementation/reporting steps completed today:
- direct square-versus-H3 geometry comparison
- corrected local overlap figure so the H3 patch is contiguous rather than a few disconnected sampled cells

### 5. Environmental transfer to H3 decided explicitly
A key design question was how to transfer environmental data from the 1° benchmark support to the H3 grid.

Decision:
- do **not** use raw square-point aggregation into H3 as the first method
- instead, use **H3 centroid sampling** from the 1° benchmark environmental field
- for the first prototype, use **nearest-neighbor assignment**
- treat `u10` and `v10` as the primary wind variables
- do not directly average wind direction as an angle

Reasoning:
- avoids empty H3 cells
- keeps the method simple and explicit
- is easier to debug and explain than aggregation plus gap-filling

### 6. Environmental assignment step completed
The first H3 environmental assignment step was implemented and reported.
Outputs now exist for:
- H3 environmental table
- summary table
- quick-look figures
- Markdown report

## Memory / workflow decisions made today

### 1. Memory architecture refined
A new memory layer was added between core memory and project memory:
- **interproject memory**

Current memory structure is now:
- core memory
- project memory
- interproject memory

### 2. Interproject memory trigger rule defined
Interproject memory should be consulted:
- at the start of a new project
- when a project resembles an older one
- at meaningful project milestones
- when the user explicitly asks for cross-project synthesis

It should **not** be read before every ordinary project discussion.

### 3. Core vs project memory clarified further
- core memory = who Leo is and how he works across projects
- project memory = scientific content specific to one project repo
- interproject memory = distilled transferable scientific essence across projects

### 4. Daily meeting-note issue identified
A process miss was identified:
- today’s project discussion had not yet been given its own dated meeting note
- this is now corrected by creating this file

## Implementation steps completed today

Ordered workflow now includes:
- `04_choose_h3_resolution.py`
- `05_build_h3_grid.py`
- `06_compare_square_h3_grids.py`
- `07_assign_environment_to_h3.py`

Associated reports and figures were generated and pushed.

## rWind lineage clarified

The `rWind` repository was checked explicitly because it is part of the methodological ancestry of the previous project.

Key takeaway:
- the old movement framework is strongly rooted in **anisotropic, directional transition modelling** rather than a simple undirected resistance surface

This matters because it supports the current sequel design choice:
- the H3 version should preserve the directional edge-cost logic
- but replace the old stack with Python + H3 rather than R + raster / gdistance

Preserve from the old lineage:
- direction-sensitive movement costs
- anisotropy
- transition-based path modelling

Replace in the sequel:
- R implementation
- square-grid dependence
- implicit package-default logic that is not written down in our own code and notes

## Standardization and distance-term decision

After checking the exact 2025 paper logic, the component standardization strategy is now clearer:
- the old model used **P99-based standardization to 0-100 SCU** for the major varying components
- distance in the old model was a **constant per-step penalty**, derived from `P50(w_cost)` rather than a spatially varying distance field

Decision for the H3 sequel:
- preserve the **P99-based standardization philosophy**
- adapt the **distance term** to true H3 edge length rather than keeping the old constant-per-step proxy

Interpretation:
- this keeps the published scaling logic where it is scientifically strong
- but treats distance as an intentional methodological refinement made possible by the H3 graph representation

## Chlorophyll treatment decision

Decision for the H3 sequel:
- follow the 2025 paper's chlorophyll treatment for the food component
- do **not** use raw chlorophyll directly in the food-cost term
- preserve the paper logic based on the log-transformed chlorophyll-derived food cost and the high-productivity cap

Interpretation:
- this keeps the food component closely aligned with the published method
- comparability is more important here than inventing a new chlorophyll transform at this stage

## Edge-environment table clarification

After building the enriched H3 edge table, the role of source versus target environmental fields was clarified explicitly.

For the first paper-faithful implementation:
- **source wind variables** (`source_u10`, `source_v10`, `source_speed`) are required because the old movement logic evaluates wind at the starting cell for a directional move
- **source chlorophyll** is required because the published food-cost logic is source-based in the extracted formulation

Additional retained field:
- **target chlorophyll** is kept in the edge table as an explicit diagnostic / future-method hook
- it is not required for the first implementation of the published method
- it is retained so that source-based and destination-based food formulations can be compared transparently later if useful

Not currently needed:
- **target wind** is not required for the first implementation and is intentionally not part of the working edge-environment table

Interpretation:
- the main model is source-based
- the extra target chlorophyll column is a transparent optional extension point, not hidden methodological drift

## Evening progress update

Substantial progress was completed on the H3 sequel pipeline.

### New implementation steps completed
- built `10_attach_environment_to_h3_edges.py`
- created the enriched edge table `data/processed/grids/h3_edge_environment_res3.csv`
- built `11_compute_h3_cost_components.py`
- created `data/processed/grids/h3_edge_cost_components_res3.csv`

### Cost-component outputs completed
The H3 edge-cost table now includes raw and standardized versions of:
- parallel wind cost
- crosswind cost
- true edge-distance cost
- food cost

### Important audit and correction
The food-cost implementation was audited against the 2025 paper.

Key correction:
- zero or non-positive chlorophyll values should not disappear after log transform
- they should remain in the food surface as high-cost cells
- implementation was corrected by flooring non-positive chlorophyll to the smallest positive chlorophyll value before log transform

Interpretation:
- this keeps the implemented food surface aligned with the paper logic that low-food cells should be expensive and high-food cells should be cheap

### Transparency outputs completed
Step 11 now also includes:
- summary tables
- example rows
- raw and standardized component histograms
- wind versus crosswind scatter
- four diagnostic maps:
  - parallel wind cost for straight northward flight
  - crosswind cost for straight northward flight
  - northward cumulative distance
  - food cost

### Clarified interpretation of maps
- the wind maps are diagnostic cell-level surfaces using a fixed northward heading for visualization only
- the actual routing graph still uses the true edge bearing for each directed move
- the northward cumulative distance map is also diagnostic and not the same thing as the actual edge-distance term used in routing

### Repository / workflow notes
- the processed H3 edge cost table is large enough for GitHub to warn about file size (about 69 MB)
- this may require later decisions about whether all large processed tables should remain in normal git history

## Important open direction
The next likely technical step is to compare the square-grid and H3-grid environmental fields more directly, then begin constructing movement costs on the H3 support.
